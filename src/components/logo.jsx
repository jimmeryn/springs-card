import React from "react";

const Logo = () => (
  <div className="logo">
    <div>
      <h1 className="logo-main">SEMA</h1>
      <span className="logo-underline"></span>
    </div>
    <h4 className="logo-sub">SPRINGS</h4>
  </div>
);

export default Logo;
